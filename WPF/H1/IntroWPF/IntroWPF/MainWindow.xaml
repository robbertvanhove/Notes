    
    
    <Window x:Class="IntroWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:IntroWPF"
        mc:Ignorable="d"
        Title="StackPanel menu" Height="350" Width="350">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10" />
            <Setter Property="Background" Value="CadetBlue" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="DemiBold" />
            <Setter Property="FontSize" Value="16" />
        </Style>

        
    </Window.Resources>
    
    <Grid>
        <StackPanel>
            <TextBlock  Text="Overzicht window lay-out WPF" Margin="30" FontSize="20" TextAlignment="Center"/>
            <Button Margin="10" Content="Grid"   Name="ButtonGrid" Click="ButtonGrid_Click"/>
            <Button Margin="10"  Content="DockPanel" Name="ButtonDock" Click="ButtonDock_Click" />
            <Button Margin="10" Content="WrapPanel" Name="ButtonWrap" Click="ButtonWrap_Click"/>
            <Button Margin="10" Name="ButtonCanvas" Click="ButtonCanvas_Click" >Canvas</Button>
        </StackPanel>


    </Grid>
</Window>
