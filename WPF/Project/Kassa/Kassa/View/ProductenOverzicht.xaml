<Window x:Class="Kassa.View.ProductenOverzicht"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Kassa.View"
        xmlns:viewmodel="clr-namespace:Kassa.ViewModel"
        mc:Ignorable="d"
        Title="ProductenOverzicht" Height="800" Width="800">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Height" Value="25" />
            <Setter Property="Width" Value="200" />
        </Style>
        <viewmodel:ProductenOverzichtViewModel x:Key="Producten" />

    </Window.Resources>
    <Grid DataContext="{StaticResource Producten}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <!-- overzicht producten-->
        <ListView ItemsSource="{Binding Producten}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Label Content="{Binding Naam}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        
    </Grid>
</Window>
